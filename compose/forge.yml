# Routine Solidity/Foundry check lanes.
#
# These services share one deliberately constrained posture:
# - no network access
# - read-only repository mount
# - read-only container filesystem
# - non-root host UID/GID
# - no Linux capabilities
# - bounded memory/PID usage
# - temporary cache/artifact paths
#
# Keep ordinary source checks here. Split a service into another compose file
# only when it needs a different trust boundary, such as RPC, browser access,
# a writable mount, or a published port.
x-forge-base: &forge_base
  # All Forge check services use the repo-owned Foundry image definition in
  # containers/foundry/Dockerfile. Compose owns runtime confinement below.
  build:
    context: ../containers/foundry
    dockerfile: Dockerfile
  working_dir: /work/dapps
  user: "${LOCAL_UID:?missing_LOCAL_UID}:${LOCAL_GID:?missing_LOCAL_GID}"
  read_only: true
  network_mode: "none"
  security_opt:
    - no-new-privileges:true
  cap_drop:
    - ALL
  pids_limit: 256
  mem_limit: 1g
  environment:
    HOME: /tmp/home
    XDG_DATA_HOME: /usr/local/share
    FOUNDRY_CACHE_PATH: /tmp/foundry-cache
    FOUNDRY_CONFIG: /work/dapps/foundry.toml
    FOUNDRY_OUT: /tmp/foundry-out
    FOUNDRY_BROADCAST: /tmp/foundry-broadcast
  tmpfs:
    - /tmp:rw,nosuid,nodev,noexec,size=512m
  volumes:
    - ..:/work:ro

services:
  # Formatting is a read-only source check.
  forge-fmt:
    <<: *forge_base
    command:
      - "sh"
      - "-eu"
      - "-c"
      - |
        set --
        for dir in */src */test; do
          if [ -d "$$dir" ]; then
            set -- "$$@" "$$dir"
          fi
        done
        if [ "$$#" -eq 0 ]; then
          printf '%s\n' 'forge-fmt: no dapp src/test directories found' >&2
          exit 2
        fi
        forge fmt --check "$@"

  # Compilation should not write artifacts into the working tree; outputs go
  # to tmpfs through FOUNDRY_OUT and FOUNDRY_CACHE_PATH.
  forge-build:
    <<: *forge_base
    command:
      - "sh"
      - "-eu"
      - "-c"
      - |
        set --
        for dir in */src; do
          if [ -d "$$dir" ]; then
            set -- "$$@" "$$dir"
          fi
        done
        if [ "$$#" -eq 0 ]; then
          printf '%s\n' 'forge-build: no dapp src directories found' >&2
          exit 2
        fi
        forge build --offline "$@"

  # ABI export is the one routine Forge lane that writes into the dapp tree.
  # Only dapps that already have both src/ and cam/ participate. The ABI
  # directory is treated as generated from direct src/*.sol files. Outputs are
  # staged in tmpfs first so a failed inspection does not erase the last good
  # ABI set.
  forge-abi:
    <<: *forge_base
    volumes:
      - ../dapps:/work/dapps:rw
    command:
      - "sh"
      - "-eu"
      - "-c"
      - |
        found=0

        for dapp in *; do
          if [ ! -d "$$dapp/src" ] || [ ! -d "$$dapp/cam" ]; then
            continue
          fi

          found=1
          abi_dir="$$dapp/cam/abi"
          staged_dir="$$(mktemp -d)"
          generated=0

          for source in "$$dapp"/src/*.sol; do
            if [ ! -f "$$source" ]; then
              continue
            fi

            contract="$$(basename "$$source" .sol)"
            forge inspect --json "$$source:$$contract" abi > "$$staged_dir/$$contract.json"
            generated=1
          done

          if [ "$$generated" -eq 0 ]; then
            printf 'forge-abi: no direct Solidity files found under %s/src\n' "$$dapp" >&2
            exit 2
          fi

          mkdir -p "$$abi_dir"
          find "$$abi_dir" -maxdepth 1 -type f -name '*.json' -delete
          for abi in "$$staged_dir"/*.json; do
            abi_name="$$(basename "$$abi")"
            mv "$$abi" "$$abi_dir/$$abi_name"
            printf 'forge-abi: wrote %s\n' "$$abi_dir/$$abi_name"
          done
        done

        if [ "$$found" -eq 0 ]; then
          printf '%s\n' 'forge-abi: no dapps with both src/ and cam/ found' >&2
          exit 2
        fi

  # Routine unit tests for every dapp. Repository/source hygiene checks live in
  # tests/checks and are wired through `make test` before this Forge lane runs.
  forge-test:
    <<: *forge_base
    command:
      - "forge"
      - "test"
      - "--offline"
      - "--match-path"
      - "*/test/unit/**/*.sol"
      - "-vvv"

  # Fuzz tests are separated as a Make target because they are usually heavier
  # than unit tests, but they keep the same offline container posture.
  forge-fuzz:
    <<: *forge_base
    command:
      - "forge"
      - "test"
      - "--offline"
      - "--match-path"
      - "*/test/fuzz/**/*.sol"
      - "-vvv"

  # Invariant tests are also heavier and often state-machine oriented. They
  # still run offline and read-only by default.
  forge-invariant:
    <<: *forge_base
    command:
      - "forge"
      - "test"
      - "--offline"
      - "--match-path"
      - "*/test/invariant/**/*.sol"
      - "-vvv"

  # Coverage is a reporting lane over the same unit-test convention as
  # forge-test, keeping it out of Soldeer's dependencies/ tree.
  forge-coverage:
    <<: *forge_base
    command:
      - "forge"
      - "coverage"
      - "--offline"
      - "--match-path"
      - "*/test/unit/**/*.sol"
