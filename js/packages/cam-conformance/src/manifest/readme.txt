Manifest conformance belongs here.

This facet should validate whole-root-manifest protocol rules that are larger
than basic parsing: canonical namespaces, route inventory, entry consistency,
route kind semantics, and cross-section invariants.

It should treat the CAM manifest as the root contract between publishers,
viewers, and agents.
