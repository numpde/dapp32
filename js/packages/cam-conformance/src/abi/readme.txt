ABI conformance belongs here.

This facet should validate the ABI resources that CAM namespaces reference:
function shape, mutability, named input/output compatibility, supported ABI
types, tuple structure, and the parts of ABI semantics that CAM route and write
calls rely on.

It should not execute contracts or depend on an EVM client.
