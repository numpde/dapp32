--- a/utils/cryptography/ECDSA.sol
+++ b/utils/cryptography/ECDSA.sol
@@ -125,2 +125,2 @@
-        (address recovered, RecoverError error, bytes32 errorArg) = tryRecover(hash, signature);
-        _throwError(error, errorArg);
+        (address recovered, RecoverError recoverError, bytes32 errorArg) = tryRecover(hash, signature);
+        _throwError(recoverError, errorArg);
@@ -134,2 +134,2 @@
-        (address recovered, RecoverError error, bytes32 errorArg) = tryRecoverCalldata(hash, signature);
-        _throwError(error, errorArg);
+        (address recovered, RecoverError recoverError, bytes32 errorArg) = tryRecoverCalldata(hash, signature);
+        _throwError(recoverError, errorArg);
@@ -161,2 +161,2 @@
-        (address recovered, RecoverError error, bytes32 errorArg) = tryRecover(hash, r, vs);
-        _throwError(error, errorArg);
+        (address recovered, RecoverError recoverError, bytes32 errorArg) = tryRecover(hash, r, vs);
+        _throwError(recoverError, errorArg);
@@ -203,2 +203,2 @@
-        (address recovered, RecoverError error, bytes32 errorArg) = tryRecover(hash, v, r, s);
-        _throwError(error, errorArg);
+        (address recovered, RecoverError recoverError, bytes32 errorArg) = tryRecover(hash, v, r, s);
+        _throwError(recoverError, errorArg);
@@ -271,11 +271,11 @@
-     * @dev Optionally reverts with the corresponding custom error according to the `error` argument provided.
+     * @dev Optionally reverts with the corresponding custom error according to the `recoverError` argument provided.
      */
-    function _throwError(RecoverError error, bytes32 errorArg) private pure {
-        if (error == RecoverError.NoError) {
+    function _throwError(RecoverError recoverError, bytes32 errorArg) private pure {
+        if (recoverError == RecoverError.NoError) {
             return; // no error: do nothing
-        } else if (error == RecoverError.InvalidSignature) {
+        } else if (recoverError == RecoverError.InvalidSignature) {
             revert ECDSAInvalidSignature();
-        } else if (error == RecoverError.InvalidSignatureLength) {
+        } else if (recoverError == RecoverError.InvalidSignatureLength) {
             revert ECDSAInvalidSignatureLength(uint256(errorArg));
-        } else if (error == RecoverError.InvalidSignatureS) {
+        } else if (recoverError == RecoverError.InvalidSignatureS) {
             revert ECDSAInvalidSignatureS(errorArg);
